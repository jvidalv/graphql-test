type TodoList @model {
  id: ID!
  title: String!
  description: String
  date: Boolean!
  todos: [Todo]
}

type Todo @model {
  id: ID!
  title: String!
  description: String
  date: Boolean!
  complete: Boolean
}
